# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /data/david/yolo11-jde/CH_MOT17_merged # dataset root dir to CrowdHuman dataset
train: images/train_mot_det # train images (relative to 'path')
val: images/val_mot_id # val images (relative to 'path')
test: # test images (optional)

# Classes
names:
  0: person
